import { myClientGeneratedGraphqlTypes } from './my-client-generated-graphql-types';

describe('myClientGeneratedGraphqlTypes', () => {
  it('should work', () => {
    expect(myClientGeneratedGraphqlTypes()).toEqual(
      'my-client-generated-graphql-types'
    );
  });
});
